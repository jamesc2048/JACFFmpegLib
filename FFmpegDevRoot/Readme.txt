If building on Windows, or need to link to a non-system FFmpeg build, put FFmpeg dev root here (or symbolic link), and it will be found by the build system.
For Windows, build yourself or use https://www.gyan.dev/ffmpeg/builds/
You should have include and lib directories at a minimum.